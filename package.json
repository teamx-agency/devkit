{
  "name": "teamx-devkit",
  "version": "2.2.1",
  "description": "TeamX Delivery OS — Hook-enforced state machine for Claude Code and OpenCode",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./opencode-plugin": "./opencode-plugin.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "files": [
    ".claude-plugin",
    "hooks",
    "scripts",
    "skills",
    "dist",
    "teamx-lib",
    "opencode-plugin.js",
    "configs/opencode"
  ],
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
