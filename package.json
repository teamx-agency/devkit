{
  "name": "teamx-devkit",
  "version": "2.0.0",
  "description": "TeamX Delivery OS — Hook-enforced state machine for Claude Code",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "files": [
    ".claude-plugin",
    "hooks",
    "scripts",
    "skills",
    "dist",
    "teamx-lib"
  ],
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
