# Secrets — nunca commitear
*.env
.env.*
secrets/
tokens/

# OS
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp

node_modules/
# dist/ is intentionally committed — the plugin serves compiled JS directly


.mcp.json